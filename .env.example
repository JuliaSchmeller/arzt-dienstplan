# Beispiel-Konfigurationsdatei für Umgebungsvariablen
# WICHTIG: Kopieren Sie diese Datei nach .env und ersetzen Sie die Platzhalter mit echten Werten
# Die .env-Datei wird NICHT in Git eingecheckt (siehe .gitignore)

# Flask SECRET_KEY - Verwenden Sie einen langen, zufälligen String
# Generieren Sie einen sicheren Schlüssel mit: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=your-secret-key-here

# FLASK_ENV - Setzen Sie auf 'production' für Produktionsumgebung
# Im Production-Modus ist SECRET_KEY zwingend erforderlich
FLASK_ENV=development

# Admin-Passwort für initiale Datenbank-Einrichtung (init_db.py)
# Wird nur beim ersten Ausführen von init_db.py verwendet
ADMIN_PASSWORD=your-secure-admin-password

# Log-Level (optional)
LOG_LEVEL=INFO
